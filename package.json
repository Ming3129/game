{
  "name": "game",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "pack:zip": "python3 -c \"import zipfile, os; zf = zipfile.ZipFile('tool.zip', 'w', zipfile.ZIP_DEFLATED); [zf.write(os.path.join(r, f), os.path.relpath(os.path.join(r, f), 'dist')) for r, _, fs in os.walk('dist') for f in sorted(fs)]; zf.close()\"",
    "preview": "vite preview --host 0.0.0.0 --port 3000",
    "lint": "vite build"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
